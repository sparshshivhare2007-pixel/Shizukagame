{
  "name": "Cutie Game",
  "description": "A Telegram word-based game bot.",
  "keywords": ["telegram", "python"],
  "repository": "https://github.com/TheChampu/CutieGame",
  "env": {
    "BOT_TOKEN": {
      "description": "A bot token from @BotFather.",
      "required": true
    },
    "MONGO_URI": {
      "description": "A MongoDB URI.",
      "required": true
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
